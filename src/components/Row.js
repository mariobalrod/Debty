import styled from "styled-components";

const Row = styled.div`
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    align-items: center;
    padding: 0px;
    margin-top: 50px;
    position: relative;
    width: 100%;
    height: auto;
`;

export default Row;