
import styled from "styled-components";

const Form = styled.form`
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    align-items: center;
    padding: 0px;
    margin-top: 50px;
    position: relative;
    width: 100%;
    height: auto;
`;

export default Form;