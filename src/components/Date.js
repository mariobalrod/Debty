import styled from "styled-components";

const Total = styled.h1`
    font-style: normal;
    font-weight: bold;
    font-size: 36px;
    line-height: 54px;

    text-align: center;

    color: #FEFEFE;
`;

export default Total;